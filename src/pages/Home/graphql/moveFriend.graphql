mutation moveFriend($id: ID!, $x: Int!, $y: Int!) {
  moveFriend(id: $id, x: $x, y: $y) {
    ok
    friend {
      id
      stats {
        position {
          x
          y
        }
      }
    }
    errors
  }
}
