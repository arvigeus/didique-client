mutation moveNotes($nickname: String!, $changes: [GridItemInput!]) {
  moveNotes(nickname: $nickname, changes: $changes) {
    ok
    items {
      id
      x
      y
      width
      height
    }
    errors
  }
}
